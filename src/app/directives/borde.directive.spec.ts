import { BordeDirective } from './borde.directive';

describe('BordeDirective', () => {
  it('should create an instance', () => {
    const directive = new BordeDirective();
    expect(directive).toBeTruthy();
  });
});
